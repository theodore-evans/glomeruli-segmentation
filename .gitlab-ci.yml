image: python:3.8

stages:
  - isort
  - lint
  - test

before_script:
  - pip install poetry
  - poetry config virtualenvs.create false
  - poetry install

tests:
  stage: test
  script:
    - poetry run python3 -m pytest

isort:
  stage: isort
  script:
    - poetry run isort .

lint:
  stage: lint
  script:
    - poetry run black .

# Static Type check:
#   stage: types
#   script:
#     - poetry run mypy .